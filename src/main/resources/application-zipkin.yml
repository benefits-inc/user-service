management:
  zipkin:
    tracing:
      # endpoint: "http://localhost:9411/api/v2/spans"
      endpoint: "http://192.168.0.11:9411/api/v2/spans"
  tracing:
    sampling:
      probability: 1.0

logging:
  pattern:
    level: "%5p [${spring.application.name},%X{traceId:-},%X{spanId:-}]"